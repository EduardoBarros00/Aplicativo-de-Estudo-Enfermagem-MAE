"use client";
import { studyCards } from "../flashcards-data";
import { Lesson, Subject, subjects, totalLessons } from "../study-data";
import { Progress, View, dueCount } from "../study-state";
import { SectionTitle, Stat, SubjectCard } from "./common";
export function Dashboard({progress,onView,onOpenSubject,onOpenLesson}:{progress:Progress;onView:(v:View)=>void;onOpenSubject:(s:Subject)=>void;onOpenLesson:(l:Lesson,s:Subject)=>void}){const done=progress.completed.length;const pct=Math.round(done/totalLessons*100);const accuracy=progress.quizTotal?Math.round(progress.quizCorrect/progress.quizTotal*100):0;const last=progress.lastLesson?subjects.flatMap(s=>s.lessons.map(l=>({s,l}))).find(x=>x.l.id===progress.lastLesson):null;const date=new Intl.DateTimeFormat("pt-BR",{weekday:"long",day:"2-digit",month:"long"}).format(new Date());return <><section className="welcome"><div><span className="eyebrow">PAINEL DE ESTUDOS</span><h1>Seu estudo de enfermagem, organizado.</h1><p>{date}. Continue do ponto em que parou e revise o que precisa de atenção.</p></div></section><section className="hero"><div><span className="pill">FBarros Enfermagem 2.0</span><h2>Estude. Pratique. Revise.</h2><p>Aulas práticas, revisão espaçada, quiz e ferramentas em um único lugar.</p><div className="hero-actions"><button onClick={()=>last?onOpenLesson(last.l,last.s):onOpenSubject(subjects[0])}>{last?"Continuar última aula":"Começar a estudar"}</button><button className="ghost" onClick={()=>onView("flashcards")}>Revisões de hoje</button></div></div><img src="/art-dashboard.svg" alt="Painel de estudos"/></section><section className="stats"><Stat value={`${pct}%`} label="Aulas concluídas" sub={`${done} de ${totalLessons}`}/><Stat value={String(dueCount(progress))} label="Revisões pendentes" sub={`${studyCards.length} flashcards no banco`}/><Stat value={`${accuracy}%`} label="Acerto nos quizzes" sub={`${progress.quizTotal} respostas`}/><Stat value={`${Math.floor(progress.studyMinutes/60)}h ${progress.studyMinutes%60}m`} label="Tempo registrado" sub="tempo de estudo"/></section><SectionTitle title="Matérias" action="Ver todas" onClick={()=>onView("materias")}/><div className="subject-grid">{subjects.slice(0,6).map(s=><SubjectCard key={s.id} subject={s} progress={progress} onClick={()=>onOpenSubject(s)}/>)}</div><div className="quick-grid"><article className="quick"><div><h3>Flashcards inteligentes</h3><p>Revise com Errei, Difícil, Lembrei ou Fácil.</p></div><button onClick={()=>onView("flashcards")}>Revisar agora</button></article><article className="quick"><div><h3>Calculadoras educacionais</h3><p>Dose, infusão e gotejamento com fórmula visível.</p></div><button onClick={()=>onView("ferramentas")}>Abrir ferramentas</button></article></div></>}
